#include <string.h>
#include <stdio.h>
#include "main.h"

int _strlen(char *s)
{
    /**int length;

    length = strlen(*s);
    //for ( length = 0; *(s + length) != '\0'; length++);
    return length;
    **/
   printf("%p", s);
}
